/**
 * Removes 'optional' attributes from a type's properties
 *
 * @breadcrumb Types
 * @public
 */
export declare type Concrete<Type> = {
    [Property in keyof Type]-?: Type[Property];
};
export declare type __ΩConcrete = any[];
