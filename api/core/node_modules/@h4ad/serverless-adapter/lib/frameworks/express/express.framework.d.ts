/// <reference types="node" />
import { IncomingMessage, ServerResponse } from 'http';
import type { Express } from 'express';
import { FrameworkContract } from '../../contracts';
/**
 * The framework that forwards requests to express handler
 *
 * @breadcrumb Frameworks / ExpressFramework
 * @public
 */
export declare class ExpressFramework implements FrameworkContract<Express> {
    /**
     * {@inheritDoc}
     */
    sendRequest(app: Express, request: IncomingMessage, response: ServerResponse): void;
}
